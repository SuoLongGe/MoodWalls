server:
  port: 8080
  address: 0.0.0.0
  tomcat:
    max-http-post-size: 15728640

spring:
  application:
    name: moodwalls-backend
  config:
    import: optional:classpath:application-local.yml
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 15MB
  codec:
    max-in-memory-size: 15MB
  datasource:
    url: jdbc:mysql://localhost:3306/moodwalls?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: hjy89607425
    driver-class-name: com.mysql.cj.jdbc.Driver
  mail:
    host: smtp.qq.com
    port: 465
    username: ${MOODWALLS_MAIL_USERNAME:}
    password: ${MOODWALLS_MAIL_PASSWORD:}
    properties:
      mail:
        smtp:
          auth: true
          ssl:
            enable: true
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

moodwalls:
  mail:
    enabled: true
    from: "MoodWalls <3528387348@qq.com>"
    code-ttl-seconds: 300
    resend-interval-seconds: 60
  jwt:
    secret: dev-only-change-me-in-application-local-yml
    expiration-ms: 604800000
  cors:
    allowed-origins: "*"
  server-host: localhost
  upload:
    dir: uploads
    max-size-bytes: 10485760
  ai:
    api-url: https://api.siliconflow.cn/v1/chat/completions
    api-key: sk-dtmdncziyfpwquhoouhmxhqocpjlkpcmxwjakneheupbbiyu
    model: deepseek-ai/DeepSeek-V3

logging:
  level:
    com.moodwalls: DEBUG
    org.hibernate.SQL: DEBUG
